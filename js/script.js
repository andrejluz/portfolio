$(document).ready(function(){
    $('.sl').slick({
      prevArrow: '.arrow_prev',
      nextArrow: '.arrow_next',
      autoplay: true,
      autoplaySpeed: 3000,
    });

    $('.testimon').slick({
      prevArrow: '.testimon__prev',
      nextArrow: '.testimon__next',
      autoplay: true,
      autoplaySpeed: 3000,
    });
  });